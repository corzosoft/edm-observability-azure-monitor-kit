# Alert: High Data Quality Exceptions

Trigger when exception count exceeds baseline by source, rule, or severity.

First checks:

- Identify top failing rules.
- Compare current exception count to normal run history.
- Sample affected records.
- Decide whether the issue is vendor data, platform logic, or expected market event volume.
